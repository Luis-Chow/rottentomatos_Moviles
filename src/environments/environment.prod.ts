// Entorno de producción. Pon aquí la URL del backend desplegado (Railway, etc.).
// Ejemplo: apiUrl: 'https://rotten-tomatos-backend-production.up.railway.app'
export const environment = {
  production: true,
  apiUrl: '',
};
