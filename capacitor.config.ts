import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.luischow.rottentomatos',
  appName: 'RottenTomatos',
  webDir: 'www',
};

export default config;
